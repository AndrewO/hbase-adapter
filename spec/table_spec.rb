require File.dirname(__FILE__) + "/spec_helper"

describe "Hbaser::Table" do
  # before do
  #   @table = Hbaser::Table.new(test_client, "test_table")
  # end
  
  it "can enable or disable a table and get its status"
  
  it "can compact tables"
  
  it "can major compact tables"
  
  it "gets a table's columns"
  
  it "gets a table's regions"
  
  it "can delete a table"
end