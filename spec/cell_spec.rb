require File.dirname(__FILE__) + "/spec_helper"

describe "HbaseAdapter::Cell" do
  it "gives access to its value and timestamp"
  
  it "can get previous versions of itself"
  
  it "it can atomically increment"
end